# 范哲齐-25361077-第三次人工智能编程作业

仓库链接: https://github.com/z7-FSE/fanzhq_Work3.git

## 项目说明

本项目对 `ICData.csv` 中的公交 IC 卡刷卡数据进行预处理、时间分布分析、线路搭乘站点统计、高峰小时系数计算、驾驶员信息导出与服务绩效排名。

主程序是 `homework.py`，数据文件是 `ICData.csv`。`范哲齐-25361077-第三次作业运行结果.ipynb` 中包含六项任务的代码、运行结果和三张图。

## 运行方法

安装 Python 3.10 或更高版本，并确保已安装 `numpy` 、`pandas` 、`matplotlib` 和 `seaborn` 。在项目根目录执行：

```bash
python homework.py
```

程序会从头到尾完成六项任务，并在当前项目中生成：

- `hour_distribution.png`
- `route_stops.png`
- `performance_heatmap.png`
- `线路驾驶员信息/1101.txt` 至 `线路驾驶员信息/1120.txt`
- `运行结果记录.txt`（完整终端输出）

## 1. 任务拆解与 AI 协作策略

我先把题目拆成六项任务，再按数据预处理、统计、绘图和文件导出的顺序完成：

1. 先读取数据，核对字段类型和记录数，再解析时间、构造 `hour` 与 `ride_stops`。
2. 将上车刷卡数据单独筛选出来，用 NumPy 布尔索引统计指定时段。
3. 先确定 `analyze_route_stops` 的硬性函数签名和返回列，再单独检查均值、标准差和误差棒。
4. 将 PHF 拆成“自动找高峰小时”、“分钟重采样”、“代入公式”三步，用打印出的分子和分母手工复算。
5. 对 1101~1120 使用固定范围循环，导出后再检查文件数是否恰好为 20。
6. 最后生成四组 Top 10 与热力图，并完成结论文字、整体运行测试和 Git 小步提交。

完成一项后先运行程序，确认数值和输出文件正常后再提交 Git。

## 2. 核心 Prompt 迭代记录

### 初代 Prompt

> 请帮我用 Python 分析公交 IC 卡数据，统计时间分布、线路平均搭乘站数、高峰小时系数和服务量排名，并画图。

### 初版问题

初代 Prompt 没有把评分点说清楚，容易引出三类不合规结果：任务2可能只使用 pandas 条件筛选而没有实质使用 NumPy；任务3可能修改题目规定的函数签名，或画出默认置信区间而非标准差；任务4可能主观假定 8:00~9:00 为高峰小时，没有先对 24 小时计数后自动识别。

### 优化后的 Prompt

> 请只在“刷卡类型=0”的有效记录上完成后续分析。任务2必须把 `hour` 转成 NumPy 数组，使用布尔索引计算 `hour < 7` 和 `hour >= 22`，柱状图直接调用 `matplotlib.pyplot`。任务3严格保留 `def analyze_route_stops(df, route_col='线路号', stops_col='ride_stops')` 及指定 docstring，使用 seaborn `barplot`，误差棒为标准差、`capsize=0.3`。任务4先对全天分小时计数并用 `argmax` 自动定位高峰小时，再对该小时分别按 5min 和 15min 重采样，按题目公式计算并打印起止时间、分子、分母和 4 位小数结果。

实际修改时，我对新版 `errorbar='sd'` 和旧版 `ci='sd'` 做了兼容，又增加了 CSV 存在性与必要字段检查。

## 3. Debug 记录

### 现象

线路条形图需要展示标准差，但 seaborn 新旧版本的参数不一致：旧版使用 `ci='sd'`，0.12 及更新版本则推荐 `errorbar='sd'`。如果只保留一种写法，可能在老师的环境中报错。

### 解决过程

我先用当前 seaborn 0.13 环境运行 `errorbar='sd'`，确认图中误差棒实际出现；然后在调用外层加入 `try/except`，当新参数不受支持时退回 `ci='sd'`。两个分支都显式传入 `capsize=0.3`，同时将 Top15 线路顺序由 `analyze_route_stops` 返回结果控制，避免绘图后名次被重新排列。最后查看输出 PNG，确认标签、柱体和误差棒没有裁切。

另外测试了 CSV 路径不存在、缺少必要字段和重复运行三种情况。重复运行时，任务5会先清理旧 txt，再重新导出 1101~1120。

## 4. 人工代码审查（任务4 PHF 核心代码）

下面是任务4的 PHF 核心代码。

```python
# argmax 返回刷卡量最大值的小时索引，不手工预设早高峰。
peak_hour = int(np.argmax(hourly_counts))
# 只保留高峰小时中的上车刷卡记录。
peak_records = boardings.loc[boardings["hour"].eq(peak_hour)].copy()
# 记录条数是两个 PHF 公式的共同分子。
peak_volume = int(len(peak_records))
# 设置并排序时间索引，保证 resample 能够按自然时间窗口分组。
peak_timeline = peak_records.set_index("交易时间").sort_index()

# 将高峰小时分成12个5分钟窗口，计算每个窗口的刷卡量。
counts_5min = peak_timeline.resample("5min").size()
# 取出最大5分钟窗口的刷卡量。
max_5_volume = int(counts_5min.max())
# PHF5 分母用 12 个5分钟窗口乘以最大窗口流量。
phf5 = peak_volume / (12 * max_5_volume)

# 将高峰小时分成4个15分钟窗口，计算每个窗口的刷卡量。
counts_15min = peak_timeline.resample("15min").size()
# 取出最大15分钟窗口的刷卡量。
max_15_volume = int(counts_15min.max())
# PHF15 分母用 4 个15分钟窗口乘以最大窗口流量。
phf15 = peak_volume / (4 * max_15_volume)
```

在本数据集上，程序自动识别的高峰小时为 09:00~10:00，刷卡量为 14961 次；最大5分钟刷卡量为 1424 次，`PHF5=0.8755`；最大15分钟刷卡量为 4060 次，`PHF15=0.9212`。两个系数都小于 1 且接近 1，数值范围符合 PHF 定义。

## 5. 运行结果核对

- 原始数据：200000 行，10 列。
- 删除 `ride_stops=0` 异常记录：40886 行。
- 预处理后有效记录：159114 行；其中上车刷卡 159113 次。
- 07:00 以前：3188 次，占 2.00%；22:00 及以后：1660 次，占 1.04%。
- 生成 `hour_distribution.png`、`route_stops.png` 和 `performance_heatmap.png`。
- `线路驾驶员信息` 中已生成 1101~1120 共 20 个文件。

### 结果案例

任务4高峰小时系数的实际输出为：

```text
高峰小时：09:00 ~ 10:00，刷卡量：14961 次
最大5分钟刷卡量（09:30~09:35）：1424 次
PHF5  = 14961 / (12 × 1424) = 0.8755
最大15分钟刷卡量（09:30~09:45）：4060 次
PHF15 = 14961 / ( 4 × 4060) = 0.9212
```

任务6的 Top1 结果为：驾驶员0服务6484人次，线路46003服务7127人次，上车站点1服务7776人次，车辆0服务11774人次。

## 6. 版本控制记录

项目使用 `main` 分支进行小步提交。可执行以下命令查看：

```bash
git log --oneline --reverse
```

提交信息包括项目建立、任务1/2、任务3、任务4、任务5、任务6及文档与 Notebook，数量不少于 6 条。
